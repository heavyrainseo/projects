# 빅데이터프로그래밍응용
 Codespace 생성 후 터미널에서 아래 명령을 실행한다.

 1. python --version
 2. pip install uv
 3. uv init
 4. uv add matplotlib pandas seaborn plotly streamlit (기타 필요한 모듈 나열)

파일을 수정하면 반드시 깃허브 서버에 업로드한다.  
작업 종료 후 바로 사용하지 않을 예정이면 생성한 codespace를 삭제한다.
